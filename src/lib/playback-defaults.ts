/** Default playback speed (1× real time). */
export const DEFAULT_PLAYBACK_SPEED = 1;

/** Default note timing tolerance in ms (matches Rust `DEFAULT_TOLERANCE_MS`). */
export const DEFAULT_TOLERANCE_MS = 130;
